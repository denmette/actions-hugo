---
id: TASK-0001.05
title: Define the GitHub Actions toolkit migration baseline for dependency upgrades
status: In Progress
assignee:
  - codex
created_date: '2026-04-07 06:39'
updated_date: '2026-04-07 08:11'
labels:
  - dependencies
  - planning
  - github-actions
dependencies: []
documentation:
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/package.json
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/jest.config.js
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/tsconfig.json
  - 'https://github.com/actions/toolkit/blob/main/packages/core/package.json'
  - >-
    https://github.com/actions/toolkit/blob/main/packages/http-client/package.json
  - >-
    https://github.com/actions/toolkit/blob/main/packages/tool-cache/package.json
  - 'https://github.com/actions/toolkit/blob/main/packages/http-client/README.md'
parent_task_id: TASK-0001
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Establish the compatibility baseline for upgrading the repository's @actions/* runtime dependencies before further implementation work proceeds. Current investigation found that replacing node-fetch with the Node built-in fetch breaks under the repository's current Jest 26 setup, and upgrading to newer @actions/core transitively introduces node:-specifier and undici-based runtime assumptions that the current test/tooling stack does not fully support. This task should determine the approved migration target for the GitHub Actions toolkit packages and document the required follow-on changes for runtime code, Jest, TypeScript, bundling, and action metadata.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The approved target versions or target major line for @actions/core, @actions/exec, @actions/io, and @actions/tool-cache are documented with rationale.
- [x] #2 The task records whether this repository will stay on CommonJS-compatible toolkit releases for now or migrate to the newer ESM-oriented toolkit packages in this upgrade initiative.
- [x] #3 Compatibility impacts on Jest, ts-jest, TypeScript, ncc bundling, and Node runtime metadata are explicitly documented so dependent tasks can implement against a fixed baseline.
- [x] #4 The Backlog dependency graph and subtask descriptions are updated to reflect the chosen migration path before additional dependency-upgrade implementation resumes.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Document the current repository compatibility baseline: action metadata uses node20, while the codebase still relies on CommonJS, TypeScript 4, Jest 26, ts-jest 26, and the current ncc packaging flow.
2. Record the approved migration direction as the conservative path: keep this initiative on the highest practical CommonJS-compatible @actions/* line and do not target the newer ESM-oriented toolkit majors in the current dependency-upgrade effort.
3. Capture the concrete compatibility impacts for downstream tasks, including built-in fetch availability in tests, node:-specifier handling, HTTP error semantics, TypeScript typing changes, and bundling/tooling implications.
4. Update the parent task and dependent subtasks so TASK-0001.01 through TASK-0001.04 implement against the conservative baseline, and create a separate follow-up task for a future migration to the newer ESM-oriented toolkit path.
5. Create a dedicated feature branch for TASK-0001.05, keep the task commit limited to planning/backlog updates, run the relevant local validation for the planning-only change, and commit with a conventional commit message.

6. Adjust the local integration-test setup so the suite can run outside GitHub Actions more reliably by providing a local RUNNER_TEMP fallback and cleaning the work directory before each test case.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-04-07: Reviewed action.yml, package.json, jest.config.js, tsconfig.json, and current worktree state before drafting the migration-baseline plan. Existing experimental runtime-upgrade edits are still present on branch chore/task-0001-01-runtime-deps and must be handled explicitly as part of this planning task.

2026-04-07: User approved the conservative toolkit baseline and requested an additional follow-up task for the newer ESM-oriented migration path.

Approved conservative baseline for this initiative: stay on the CommonJS-compatible GitHub Actions toolkit line represented by @actions/core 1.x, @actions/exec 1.x, @actions/io 1.x, and @actions/tool-cache 2.x rather than upgrading to the newer ESM-oriented toolkit majors now.

Rationale: the repository is currently packaged as CommonJS with TypeScript 4, Jest 26, ts-jest 26, and ncc 0.38. Investigation showed that built-in fetch is not available in the current Jest environment and newer toolkit packages introduce node:-specifier and undici/http-client assumptions that should be handled in a separate migration.

Downstream implementation guidance: TASK-0001.01 should remove node-fetch v2-specific code using a CommonJS-compatible HTTP approach, preferably aligned with the toolkit HTTP client behavior, without requiring the newer ESM toolkit major line.

Future-state planning: TASK-0001.06 tracks the later migration to the newer ESM-oriented @actions/* toolkit majors after the conservative dependency cleanup is complete and stable.

Local validation on 2026-04-07: npm test initially failed in sandbox because integration tests create /Users/macs/actions_hugo and expect runner-like environment variables.

Local validation on 2026-04-07: rerunning with RUNNER_TEMP=/tmp outside the sandbox showed the earlier environment assertion was resolved, but several integration tests still exceeded the current 30000 ms timeout while downloading/extracting Hugo binaries. This appears to be an existing test-suite/runtime constraint rather than a planning-task regression.

Validation takeaway for downstream tasks: runtime/tooling tasks should treat integration-test environment assumptions and timeouts as a known repo constraint when verifying dependency changes.

2026-04-07: Added explicit conventional commit 40d680e (chore(backlog): define conservative toolkit baseline) on the planning branch because Backlog MCP task-update commits do not use conventional commit format.

2026-04-07: Acceptance criteria are complete. Task remains In Progress instead of Done because the repository's existing integration tests did not complete cleanly under local validation; the timeout/runtime issue was documented for follow-up work.

2026-04-07: User requested a small local-development test-setup fix so the planning task can validate cleanly. Scope is limited to integration-test harness setup for local runs.

2026-04-07: Updated __tests__/main.test.ts for local integration runs by cleaning the work directory before each test and defaulting RUNNER_TEMP to os.tmpdir() when it is unset.

2026-04-07: Validation succeeded with env RUNNER_TEMP=/tmp npm test -- --runInBand. Result: 5/5 suites passed and 16/16 tests passed.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Defined the conservative GitHub Actions toolkit migration baseline for the current dependency-upgrade initiative and updated dependent backlog tasks to implement against that decision. The approved path keeps the repository on the CommonJS-compatible toolkit line for now, with TASK-0001.06 created for a later migration to the newer ESM-oriented toolkit packages.

Updated __tests__/main.test.ts so local integration runs clean the shared work directory before each test and provide a RUNNER_TEMP fallback when that runner variable is unset. Validation then succeeded with env RUNNER_TEMP=/tmp npm test -- --runInBand, producing 5/5 passing suites and 16/16 passing tests.

Key outputs: backlog task hierarchy and plans were updated to reflect the conservative baseline, the future ESM migration was split into its own task, and the local integration test harness is now stable for repeated non-runner execution.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Feature branch created for the task
- [x] #2 Relevant local tests pass before completion
- [x] #3 Changes are committed with conventional commit messages
- [x] #4 Backlog task notes and status are kept current during execution
<!-- DOD:END -->
