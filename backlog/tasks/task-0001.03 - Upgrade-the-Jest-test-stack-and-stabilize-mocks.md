---
id: TASK-0001.03
title: Upgrade the Jest test stack and stabilize mocks
status: Done
assignee:
  - codex
created_date: '2026-04-07 05:49'
updated_date: '2026-04-07 11:16'
labels:
  - dependencies
  - testing
dependencies:
  - TASK-0001.05
  - TASK-0001.02
documentation:
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/package.json
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/__tests__
parent_task_id: TASK-0001
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Refresh the test runner stack from the older Jest 26-era packages to current supported versions and make the test suites pass with the newer behavior. This includes jest, ts-jest, jest-circus if still needed, @types/jest, and supporting libraries such as nock where version upgrades affect test semantics. The repository has tests in __tests__/ that exercise installation flow and fetch error handling; this task should keep those tests meaningful after the upgrade.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Jest-related dependencies in package.json are upgraded to a current compatible set with package-lock.json updated.
- [x] #2 The test suites under __tests__/ pass on the upgraded stack without suppressing legitimate failures.
- [x] #3 Outdated test-only constructs or mocks that are incompatible with the newer Jest ecosystem are cleaned up rather than left as brittle compatibility shims.
- [x] #4 Any required test configuration changes are committed in the same task.
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
TASK-0001.05 fixed the baseline for this task: modernize the Jest stack to support the conservative CommonJS-compatible runtime/tooling path first. The newer ESM-oriented toolkit migration is tracked separately in TASK-0001.06.

2026-04-07: Implementation started on branch chore/task-0001-03-jest-upgrade from rebased main. Current baseline is Jest 26.6.3 with ts-jest 26.5.3, jest-circus 26.6.3, @types/jest 26.0.24, and local node-compat shims in jest.config.js that were added to support the updated runtime dependency tree under the older test runner.

2026-04-07: Upgraded the test stack to Jest 29.7.0, ts-jest 29.4.9, @types/jest 29.5.14, and nock 13.5.6. The final tree no longer needs a standalone jest-circus dependency because Jest 29 provides that internally where needed.

2026-04-07: The Jest 29 migration showed that the old node:-specifier remapping layer was no longer required. Removed the dead crypto/events/stream/util shim files and simplified jest.config.js to keep only the globals setup file required for undici's AbortSignal/Event assumptions in the Node test environment.

2026-04-07: TypeScript validation initially exposed an Istanbul declaration mismatch after the Jest upgrade. Fixed it by upgrading @types/istanbul-lib-report to the compatible 3.x line instead of weakening compiler settings.

2026-04-07: Local validation passed with npm run lint, npm run tsc, and env RUNNER_TEMP=/tmp npm test -- --runInBand (5/5 suites, 16/16 tests).
<!-- SECTION:NOTES:END -->
