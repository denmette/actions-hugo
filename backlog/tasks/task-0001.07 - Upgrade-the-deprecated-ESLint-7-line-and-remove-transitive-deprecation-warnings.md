---
id: TASK-0001.07
title: Upgrade the deprecated ESLint 7 line and remove transitive deprecation warnings
status: Done
assignee: []
created_date: '2026-04-08 10:55'
updated_date: '2026-04-08 12:47'
labels:
  - dependencies
  - tooling
  - eslint
dependencies:
  - TASK-0001.02
  - TASK-0001.06
documentation:
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/package.json
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/.eslintrc.json
  - /Users/macs/Coding/Personal/github-actions/actions-hugo/renovate.json
parent_task_id: TASK-0001
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Upgrade the repository off the deprecated ESLint 7 toolchain line so npm install no longer emits the known ESLint 7 deprecation warnings and related transitive @humanwhocodes package deprecation notices. This should be treated as a deliberate follow-up modernization task rather than an ad hoc npm audit fix, because the change will likely require a coordinated upgrade of eslint, @typescript-eslint, and related configuration.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The repository no longer depends on the deprecated ESLint 7 line in package.json/package-lock.json.
- [x] #2 npm install no longer emits the current ESLint 7 and @humanwhocodes deprecation warnings.
- [x] #3 Any required eslint/@typescript-eslint/config migration work is handled coherently rather than via forced audit updates.
- [x] #4 The resulting lint and type-check workflow still passes locally and in CI.
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-04-08: This task was created after local npm install output showed the expected warnings for eslint@7.32.0 and its transitive @humanwhocodes/config-array and @humanwhocodes/object-schema packages. These warnings are currently tolerated because the repository intentionally stopped at a conservative compatible tooling baseline first.

This follow-up should not be handled with npm audit fix --force. It should be implemented as an intentional tooling migration, likely alongside newer eslint/@typescript-eslint work that fits the broader post-conservative modernization path.

2026-04-08: Task started on branch `chore/task-0001-07-eslint-upgrade`.

Compatibility check before implementation:
- `eslint-plugin-jest@29.15.1` supports `eslint ^8.57.0 || ^9.0.0 || ^10.0.0`.
- `@typescript-eslint/eslint-plugin@8.58.1` supports `eslint ^8.57.0 || ^9.0.0 || ^10.0.0` and `typescript >=4.8.4 <6.1.0`.

Planned migration target:
- `eslint` 9.x
- `@typescript-eslint/parser` and `@typescript-eslint/eslint-plugin` 8.58.1
- `eslint-plugin-jest` 29.15.1

This task should also migrate repository lint configuration as needed for ESLint 9 compatibility.

Completed on April 8, 2026 on branch `chore/task-0001-07-eslint-upgrade`.

Changes applied:
- Upgraded `eslint` to 9.39.4, `@typescript-eslint/parser` and `@typescript-eslint/eslint-plugin` to 8.58.1, `eslint-plugin-jest` to 29.15.1, and added `@eslint/js` for the flat-config baseline.
- Replaced the legacy `.eslintrc.json` file with `eslint.config.js` to match the ESLint 9 configuration model.
- Fixed the resulting rule migrations in tests by replacing `toThrowError()` with `toThrow()`.
- Replaced the constant enums in `src/constants.ts` with readonly constant objects so the upgraded `@typescript-eslint` rule set no longer flags duplicate enum values.

Validation:
- `npm install`
- `npm run lint`
- `npm run tsc`
- `env RUNNER_TEMP=/tmp npm test -- --runInBand`

Confirmation note:
- A final `npm install` after the lockfile update no longer emitted the previous `eslint@7.32.0`, `@humanwhocodes/config-array`, or `@humanwhocodes/object-schema` deprecation warnings.
<!-- SECTION:NOTES:END -->
